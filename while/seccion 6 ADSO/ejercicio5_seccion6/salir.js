let Aviso= alert ("Se mostrara el ejercicio 5 de la seccion 6. ")
let salir = false;
let respuesta = prompt("¿Desea salir? (S/N)");
while (!salir) {
  if (respuesta === "S" || respuesta === "s") {
    salir = true;
  }
}
